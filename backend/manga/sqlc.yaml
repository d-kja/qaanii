version: "2"
sql: 
  - engine: "sqlite"
    queries: "./internals/infra/database/queries.sql"
    schema: "./cmd/schemas.sql"

    gen:
      go:
        package: "db"
        out: "./internals/infra/database/db"

